import compose from 'recompose/compose'
import { pure } from 'recompose'

import AsyncCasesDesktop from '../../components/Cases/Desktop/AsyncCasesDesktop'

export default compose(pure)(AsyncCasesDesktop)
